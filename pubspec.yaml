name: scalex
description: A better alternative to flutter_screenutil that works great for web, mobile, and desktop. Provides simple syntax like 4.w, 4.h, 4.sp with smart scaling that keeps desktop layouts fixed.
version: 1.0.0
homepage: https://github.com/yourusername/scalex
repository: https://github.com/yourusername/scalex
issue_tracker: https://github.com/yourusername/scalex/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

